name: My First Workflow
on:
  push:

jobs:
  hello-job
    runs-on:ubuntu-latest

  steps:
    - uses: actions/checkout@v3
    
    -name: Hello World
    run: echo "Hello, World!"

    - name: Show Files
        run: ls -la
    