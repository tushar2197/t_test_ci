describe('Home page', () => {
  test('should be defined', () => {
    // Basic test to verify the test suite runs
    expect(true).toBe(true);
  });

  test('document should exist', () => {
    expect(document).toBeDefined();
  });
});
